def c_to_f(input: int):
    """
    Converts Celsius (input) into Fahrenheit (output).

    Parameters:
    input (required): A number. Assumed to be in Metric units.
    """
    return (input * 1.8) + 32
